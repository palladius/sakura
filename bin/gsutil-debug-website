#!/bin/bash

SITE=${1:-https://logscribe.commondatastorage.googleapis.com}

curl -v -H "Authorization: Bearer $(gsutil-get-oauth2-token)" "$SITE"
RET=$?
echo
echo "= Connection to $SITE returned: $RET ="
exit $RET
